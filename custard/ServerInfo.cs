namespace custard {
    public static class ServerInfo {
        public const string API_VERSION = "1";
        public const string ROUTE_PREFIX = "/custard";
        public const string API_PREFIX = ROUTE_PREFIX + "/api/v" + API_VERSION;
    }
}